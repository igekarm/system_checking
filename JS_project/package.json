{
  "name": "db-alert-workspace",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "workspaces": ["app", "desktop", "pg-tools", "packages/*"],
  "scripts": {
    "dev": "concurrently -n api,app,pg-tools \"npm:dev:api -w app\" \"npm:dev -w app\" \"npm:dev -w pg-tools\"",
    "dev:desktop": "concurrently -k -s first -n api,ui,desktop \"npm:dev:api -w app\" \"npm:dev -w app\" \"npm:dev -w desktop\"",
    "build:desktop": "npm run build -w app && npm run build -w desktop",
    "build": "npm run build -ws --if-present",
    "test": "npm run test -ws --if-present",
    "check": "npm run test && npm run build"
  },
  "devDependencies": { "concurrently": "^9.2.1" },
  "engines": { "node": ">=22" }
}
